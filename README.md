# part-3
**requiremnts.txt is not uploaded as txt file**
Churn Prediction Model &amp; Model Card

**churn_model.ipynb**

This notebook should show:

data loading,
feature preparation,
leakage checks,
train/validation/test split,
baseline model,
stronger model,
evaluation metrics,
threshold selection,
feature importance/interpretability,
final model saving.

**metrics.json**

This file should contain key model metrics such as:

accuracy,
precision,
recall,
F1-score,
ROC-AUC or PR-AUC,
confusion matrix values,
selected threshold.


**error_analysis.md**

This report must include:

false positive analysis,
false negative analysis,
at least 10 specific customer examples,
what the business risk is for each type of error.

**model_card.md**

It explains the model 

**model.pkl**

Saved final trained model or pipeline.
